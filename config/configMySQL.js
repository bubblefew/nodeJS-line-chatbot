const config = {
  db: {
    host: "localhost",
    port: "3333",
    user: "root",
    password: "root",
    // requestTimeout: 30000,
    database: "IS",
    charset: "utf8mb4",
  },
};

module.exports = config;
